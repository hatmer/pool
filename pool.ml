(* pool *)

let my_addresses =
      Base58.Bitcoin.of_string_exn "1LMDxKTXMQ7jqRTfjbq9mHkrxTo5B5PBwB"



(* fetch info from network: run full node *)


(* fxn to create hashable plaintext *)
let hashable_msg_of (txs : string) () : 


(* map tasks to workers via iteration, best effort strategy *)

let workers = {}
let num_workers = 0

let start_threads (workers : List) : (failures : List) =
    (* spawn deferred and map (>>|) onto the close fxn *)
    push


(* report failures *)



(* run server to await results *)
